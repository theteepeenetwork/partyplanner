# Copy this file to `.env` in the project root (same folder as `spark`).
# CodeIgniter loads `.env` automatically; values override app/Config/Database.php defaults.
#
# If you see: Access denied for user '...'@'localhost' (using password: YES)
# then database.default.username / database.default.password do not match MySQL.

#--------------------------------------------------------------------
# DATABASE
#--------------------------------------------------------------------
database.default.hostname = localhost
database.default.database = event_marketplace
database.default.username = root
database.default.password =
database.default.DBDriver = MySQLi
database.default.port = 3306
